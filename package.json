{
  "private": true,
  "workspaces": [
    "packages/tsconfig",
    "apps/api"
  ],
  "scripts": {
    "check-format": "prettier --check .",
    "dev": "echo hi",
    "format": "npx prettier . --write",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "prettier": "3.5.3"
  }
}
